<!DOCTYPE html>
<html>
<head>
  <title>Blog Rails App</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	<div class="container-fluid">
		<nav class="navbar navbar-default barra" role="navigation">
		  <div class="container-fluid">
		    <!-- Brand and toggle get grouped for better mobile display -->
		    <div class="navbar-header">
		      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
		        <span class="sr-only">Toggle navigation</span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		      </button>
		      <h3 style= "display: inline-block" class="navbar-text"><%= link_to 'Blog Rails APP', articles_path, :class => "eslogan" %><small> Manage your articles</small></h3>
		      
		    </div>
		    <!-- Collect the nav links, forms, and other content for toggling -->
		    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
				<% if user_signed_in? %>                
                	<%= link_to "Salir", destroy_user_session_path, method: :delete, :class => "button boton btn btn-warning navbar-right navbar-btn" %>
                	<%= image_tag Gravatar.new(current_user.email).image_url(s: 35), :class => "navbar-btn boton navbar-right" %>
              	<% else %>
		    		<%= link_to "Sign Up", new_user_registration_path, class: "btn boton btn-default navbar-right navbar-btn" %>
		    		<%= link_to "Log In", new_user_session_path, class: "btn boton btn-warning navbar-right navbar-btn" %>
              	<% end %>			
				<%= link_to 'View All Articles', articles_path, :class => "button boton btn btn-info navbar-right navbar-btn" %>
				<% if @mostrar_editar %>
					<%= link_to 'Edit Article', edit_article_path(@article), :class => "button boton btn btn-warning navbar-right navbar-btn" %>
				<% end %>
				<% if user_signed_in? %> 
					<%= link_to 'New Article' , new_article_path, :class => "button boton btn btn-success navbar-right navbar-btn" %>
				<% end %>
		    </div><!-- /.navbar-collapse -->

		  </div><!-- /.container-fluid -->
		</nav>
	</div>
	
	<% if flash[:notice] %>
		<p class="alert notice alert-info"><%= notice %></p>
	<% end %>
	<% if flash[:alert] %>
		<p class="alert alert-warning"><%= alert %></p>
	<% end %>

	<div class="container-fluid">
		<%= yield %>
	</div>
</body>
</html>
